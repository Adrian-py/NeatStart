const SecondSection = () => {
  // chrome.topSites.get((data) => {
  //   console.log(data);
  // })

  return (
    <section className="second-section"></section>
  )
};

export default SecondSection;